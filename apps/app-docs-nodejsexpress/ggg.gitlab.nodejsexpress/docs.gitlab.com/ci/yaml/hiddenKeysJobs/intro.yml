# If you want to temporarily ‘disable’ a job, rather than 
# commenting out all the lines where the job is defined:

#hidden_job:
#  script:
#    - run test

# you can instead start its name with a dot (.) 
# and it will not be processed by GitLab CI
.hidden_job:
  script:
    - run test
